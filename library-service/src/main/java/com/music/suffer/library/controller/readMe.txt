Я поймал очередной тупняк. если мы делаем какой нибудь запрос по типу /getAlbum и тд, то все просто, возвращается альбом.

Теперь такая ситуация. Например, на форму редактирования комментария поступает гет запрос,
который по сути (в твоем старом приложении-монолите) только закидывает в модель
этот комментарий и возвращает на страницу редактирования. Я не пойму, какую роль эта функция должна выполнять в этом
конкретном микросервисе(что то мне подсказывает, что ее функциональность не должна меняться от слова совсем,
т.е. нужно так и оставить)

а первопричина моего непонимания в том, что до меня до сих пор не дошло, как взаимодействуют между собой сервисы.
типа запустить один сервис на одном порту и получить с него что то это ладно.
но в каких именно местах один сервис делает запрос к другому,
конкретно в нашем случае - каким образом запрос попадает к library-service, и что этот lib-serv должен отдать
